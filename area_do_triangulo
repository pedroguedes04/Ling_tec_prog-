#include <stdio.h>

int main() {
    int base, altura, area;

    printf("Digite a base: ");
    scanf("%d", &base);

    printf("Digite a altura: ");
    scanf("%d", &altura);

    area = (base * altura) / 2;

    printf("Area = %d\n", area);

    return 0;
}
